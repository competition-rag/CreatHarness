# Manual Adapters

Place manually maintained adapter modules in this directory.

The rule assembler copies this directory into the assembled package as
`adapt_rules`. It does not create, plan, or insert adapter nodes automatically.
